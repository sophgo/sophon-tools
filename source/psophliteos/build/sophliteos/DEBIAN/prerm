#!/bin/bash
# sophliteos deb prerm：卸载/升级前停服务。
# $1: remove/upgrade/deconfigure
systemctl stop sophliteos 2>/dev/null || true
if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
  systemctl disable sophliteos 2>/dev/null || true
fi
exit 0
